using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoleMoveSet : MonoBehaviour
{
    //CharacterController role;
    //Vector3 moveV;
    //public float g = 8;
    //// Start is called before the first frame update
    void Start()
    {
        //role = GetComponent<CharacterController>();
    }

    //void Move()
    //{
    //    if(role.isGrounded)
    //    {
    //        moveV = new Vector3(Input.GetAxis("Horizontal"),0,Input.GetAxis("Vertical"));
    //    }
    //    moveV -= Vector3.down * g * Time.deltaTime;
    //    role.Move(moveV * Time.deltaTime);
    //}
    // Update is called once per frame
    void Update()
    {
     //   Move();
    }
}
